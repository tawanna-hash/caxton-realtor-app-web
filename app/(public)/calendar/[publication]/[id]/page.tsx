import CalendarDetailClient from './CalendarDetailClient';

export const metadata = { title: 'Event — Realty News Now' };

export default function CalendarDetailPage() {
  return <CalendarDetailClient />;
}
