// lib/contacts.ts
//
// Public contact numbers for Realty News Now. Centralized so a number change
// is one edit. Used by the detail-page floaters ("Contact" pill) and the
// advertise surfaces.

// RNN office / ad-sales line (Doren). Used as the "Contact" CTA on listing,
// community, and builder detail floaters — listings carry no per-home sales
// phone, so reader inquiries route here.
export const CONTACT_TEL = '+15125143141';
